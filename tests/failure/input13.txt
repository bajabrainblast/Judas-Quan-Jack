(define-fun f int
    (let
        (x getint)
        (let
            (y getint)
            (if (< y x) x y)
        )
    )
)
(print
    (let
        (x 
            (let
                (z (get-int))
                (if (< z x) x z)
            )
        ) 
        (let
            (y 
                (let
                    (a (get-int)) 
                    (let
                        (b (get-int))
                        (if (< a b) b a)
                    )
                )
            )
            (+ x y)
        )
    )
)